# WasteExchange Backend Environment Variables

# Backend Configuration
PORT=8000
HOST=0.0.0.0

# ===== SUPABASE CONFIGURATION =====
# Get these values from your Supabase project dashboard:
# 1. Go to https://app.supabase.com
# 2. Select your project
# 3. Click "Settings" > "API"
# 4. Copy the PROJECT URL (format: https://xxxxx.supabase.co)
# 5. Copy the ANON PUBLIC KEY (NOT the service_role secret)

# IMPORTANT: Use the ANON PUBLIC KEY, not the service_role secret!
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-public-key-here

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# ===== DATABASE TABLES REQUIRED =====
# The backend expects these tables in your Supabase database:
# - users (id, email, company_name, wallet_address, created_at)
# - waste_listings (id, seller_id, title, description, category, quantity_kg, remaining_kg, price_per_kg, location_lat, location_long, embedding, status, created_at)
# - buyer_needs (id, buyer_id, looking_for, embedding, active, created_at)
# - transactions (id, listing_id, buyer_id, seller_id, quantity_bought, total_price, status, created_at)

# If SUPABASE_URL and SUPABASE_KEY are not set, the backend will use in-memory storage
